# Base URL of the Django API (see backend/.env.example — CORS already allows
# http://localhost:5173 for local dev). Without this set, the contact form
# cannot reach the backend and will show an error rather than a fake success.
VITE_API_BASE_URL=http://localhost:8000
